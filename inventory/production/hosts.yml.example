---
# Production Environment Inventory
# Replace IP addresses and credentials with your production server details
all:
  children:
    webservers:
      hosts:
        production-01:
          ansible_host: YOUR_SERVER_IP
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
    
    dbservers:
      hosts:
        production-01:
          ansible_host: YOUR_SERVER_IP
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
    
    monitoring:
      hosts:
        production-01:
          ansible_host: YOUR_SERVER_IP
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
